%//////////////// Schaffer ///////////////////////////

    z = Schaffer(x(length(x)),x(1));
    for i = 2: length(x)
        z = z + Schaffer(x(i-1),x(i));
    end

%/////////////////////////////////////////////////////